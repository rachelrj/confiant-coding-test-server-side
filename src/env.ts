/*
  Change this to the path to the SQLite database file you want to connect with
 */
export const dbFilePath = "path/to/your/db";

/*
    Use
    export const dbFilePath = ":memory:";
    if you don't have a SQLite database on your local file system
 */
